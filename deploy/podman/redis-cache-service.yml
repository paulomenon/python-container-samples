version: "3.8"

services:
  app:
    build: ../../apps/redis-cache-service
    ports:
      - "5000:5000"
    environment:
      REDIS_URL: redis://redis:6379
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
