# Apply to your OpenShift project, for example:
#   oc project <your-namespace>
#   oc apply -f url-shortener-api.yml
# Or set metadata.namespace on each resource (uncomment and replace placeholder).
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: url-shortener-api
  # namespace: <your-namespace>
  labels:
    app: url-shortener-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: url-shortener-api
  template:
    metadata:
      labels:
        app: url-shortener-api
    spec:
      containers:
        - name: url-shortener-api
          image: url-shortener-api:latest
          ports:
            - name: http
              containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: url-shortener-api
  # namespace: <your-namespace>
  labels:
    app: url-shortener-api
spec:
  type: ClusterIP
  selector:
    app: url-shortener-api
  ports:
    - name: http
      port: 5000
      targetPort: http
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: url-shortener-api
  # namespace: <your-namespace>
  labels:
    app: url-shortener-api
spec:
  to:
    kind: Service
    name: url-shortener-api
    weight: 100
  port:
    targetPort: http
