# Deploy with:
#   oc new-project python-samples    (if needed)
#   oc apply -f <filename>.yml
#   oc start-build <app-name>        (trigger the first build)
#   oc get builds                    (watch build progress)
#   oc get route <app-name>          (get the URL)
---
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: flask-web-api
  labels:
    app: flask-web-api
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: flask-web-api
  labels:
    app: flask-web-api
spec:
  source:
    type: Git
    git:
      uri: https://github.com/paulomenon/python-container-samples.git
      ref: main
    contextDir: apps/flask-web-api
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Containerfile
  output:
    to:
      kind: ImageStreamTag
      name: flask-web-api:latest
  triggers:
    - type: ConfigChange
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-web-api
  labels:
    app: flask-web-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask-web-api
  template:
    metadata:
      labels:
        app: flask-web-api
      annotations:
        image.openshift.io/triggers: >-
          [{"from":{"kind":"ImageStreamTag","name":"flask-web-api:latest"},"fieldPath":"spec.template.spec.containers[?(@.name==\"flask-web-api\")].image"}]
    spec:
      containers:
        - name: flask-web-api
          image: image-registry.openshift-image-registry.svc:5000/python-samples/flask-web-api:latest
          ports:
            - name: http
              containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: flask-web-api
  labels:
    app: flask-web-api
spec:
  type: ClusterIP
  selector:
    app: flask-web-api
  ports:
    - name: http
      port: 5000
      targetPort: http
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: flask-web-api
  labels:
    app: flask-web-api
spec:
  to:
    kind: Service
    name: flask-web-api
    weight: 100
  port:
    targetPort: http
