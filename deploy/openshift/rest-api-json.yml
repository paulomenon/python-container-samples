# Deploy with:
#   oc new-project python-samples    (if needed)
#   oc apply -f <filename>.yml
#   oc start-build <app-name>        (trigger the first build)
#   oc get builds                    (watch build progress)
#   oc get route <app-name>          (get the URL)
---
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: rest-api-json
  labels:
    app: rest-api-json
---
apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: rest-api-json
  labels:
    app: rest-api-json
spec:
  source:
    type: Git
    git:
      uri: https://github.com/paulomenon/python-container-samples.git
      ref: main
    contextDir: apps/rest-api-json
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: rest-api-json:latest
  triggers:
    - type: ConfigChange
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rest-api-json
  labels:
    app: rest-api-json
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rest-api-json
  template:
    metadata:
      labels:
        app: rest-api-json
      annotations:
        image.openshift.io/triggers: >-
          [{"from":{"kind":"ImageStreamTag","name":"rest-api-json:latest"},"fieldPath":"spec.template.spec.containers[?(@.name==\"rest-api-json\")].image"}]
    spec:
      containers:
        - name: rest-api-json
          image: image-registry.openshift-image-registry.svc:5000/python-samples/rest-api-json:latest
          ports:
            - name: http
              containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: rest-api-json
  labels:
    app: rest-api-json
spec:
  type: ClusterIP
  selector:
    app: rest-api-json
  ports:
    - name: http
      port: 5000
      targetPort: http
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: rest-api-json
  labels:
    app: rest-api-json
spec:
  to:
    kind: Service
    name: rest-api-json
    weight: 100
  port:
    targetPort: http
